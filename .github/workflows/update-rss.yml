name: Générer flux RSS quotidiens

on:
  schedule:
    # Tous les jours à 12h (UTC)
    - cron: '0 12 * * *'
  workflow_dispatch:

jobs:
  generate-rss:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GH_TOKEN }}
          fetch-depth: 0
      
      - name: Switch to main branch
        run: |
          git checkout main
          git pull --rebase origin main
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
      
      - name: Installer dépendances
        run: npm install axios
      
      - name: Générer flux RSS
        run: node generate-rss.js ${{ secrets.NEWSAPI_KEY }}
      
      - name: Commit et push les changements
        run: |
          git config user.name "RSS Generator"
          git config user.email "rss@portfolio.local"
          git add feed-*.xml
          if ! git diff --cached --quiet; then
            git commit -m "📡 Mise à jour flux RSS"
            git push origin main
          else
            echo "Aucun changement RSS à pousser."
          fi
